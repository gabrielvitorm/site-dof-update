# Deprecated: use the monorepo root `.env` (from `.env.example`).
# Kept only as a pointer for older docs/scripts.

# Backend-only Meta CAPI settings belong in the runtime environment, never in React/Vite.
META_CAPI_ENABLED=false
META_PIXEL_ID=
META_CAPI_ACCESS_TOKEN=
META_CAPI_API_VERSION=v20.0
META_CAPI_TIMEOUT_MS=1500
